const Navbar = ({ width }) => {

    return (
        <section className={`w-full px-3 py-2 flex items-center border-b border-gray-100 font-semibold`} style={{ height: `${width}px` }}>
            MediQ
        </section>
    )
}

export default Navbar